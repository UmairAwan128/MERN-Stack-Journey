1. installed express,mongoose,nodemon
2. installed dotenv package (which is for creating .env file using whcih we can hide sensitive code like MongoDB connection     string and hence its not uploaded on git Git)
3. installed body-parser (it is a middleware which parses the request body content to JSON format for us)
4. installed cors (it is a middle which makes our api so it can be accessed accoros our domain like accross localhost:5000 as localhost:3000 need this)
